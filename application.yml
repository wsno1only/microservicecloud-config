spring:
  profiles:
    active:
    - dev

---
spring:
  profiles: dev
  application:
        name: microservicecloud-atguigu-dev

---
spring:
  prifile: test
  application:
    name: microservicecloud-config-atguigu-test
	